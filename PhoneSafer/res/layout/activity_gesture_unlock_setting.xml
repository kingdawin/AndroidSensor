<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bitmap_book_read_chapterlist_repeat" >

    <!-- start_background 图片太大，加载速度慢 -->
    <!-- android:background="#0000" />  android:layout_margin="20dp" -->

    <com.hy2014.phonesafer.gestureLock.GestureLockView
        android:id="@+id/gv"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true" />

    <TextView
        android:id="@+id/tv_info"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@id/gv"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="20dp"
        android:textColor="#ff0000"
        android:textSize="20dp"
        android:visibility="visible" />
    <!-- 读秒-提示 -->

    <TextView
        android:id="@+id/tv_read_second"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/gv"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="20dp"
        android:text="请解锁"
        android:textColor="#ff0000"
        android:textSize="30dp"
        android:visibility="invisible" />

</RelativeLayout>